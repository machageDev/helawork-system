class: TimeLog
table: time_log
fields:
  id: int?
  workerId: int, relation(parent=user)
  taskId: int, relation(parent=task)
  startedAt: DateTime
  endedAt: DateTime?
  durationMinutes: int?
  createdAt: DateTime?
