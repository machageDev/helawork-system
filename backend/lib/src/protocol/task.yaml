class: Task
table: task
fields:
  id: int, primary, auto
  title: String
  description: String
  deadline: DateTime
  assignedTo: int, relation(user)
  status: String # 'pending', 'in_progress', 'completed', 'approved', 'rejected'
  createdAt: DateTime
